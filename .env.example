# Default uses SQLite (async) → runs immediately
DATABASE_URL=sqlite+aiosqlite:///./app.db
ECHO_SQL=true

# --- To use MSSQL (async) ---
# 1) Install Microsoft ODBC Driver 18 for SQL Server
# 2) pip install pyodbc aioodbc
# 3) Use ONE of the two styles below:

# Option A: DSN-less (often works, but can be picky on Windows)
# NOTE: URL-encode any password special chars (e.g., '@' → %40)
# DATABASE_URL=mssql+aioodbc://USER:PASSWORD@localhost:1433/DBNAME?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes&Encrypt=no

# Option B (more reliable on Windows): Create a System DSN named WMS, then:
# DATABASE_URL=mssql+aioodbc://USER:PASSWORD@WMS
